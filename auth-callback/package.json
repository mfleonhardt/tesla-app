{
  "name": "tesla-auth-callback",
  "version": "0.0.0",
  "main": "callback.js",
  "scripts": {
    "build": "esbuild callback.ts --bundle --platform=node --outfile=build/callback.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "@types/node": "^22.13.10",
    "esbuild": "0.25.1"
  }
}
