{
  "name": "tesla-auth-callback",
  "version": "0.0.0",
  "main": "callback.js",
  "scripts": {
    "build": "esbuild src/callback.ts --bundle --platform=node --outfile=build/callback.js",
    "dev": "nodemon --watch '*.ts' --exec 'ts-node' dev-server.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "@types/node": "^22.13.10",
    "esbuild": "0.25.1",
    "nodemon": "^3.1.0",
    "ts-node": "^10.9.2"
  },
  "dependencies": {
    "cookie": "^1.0.2",
    "dotenv": "^16.4.7"
  }
}
